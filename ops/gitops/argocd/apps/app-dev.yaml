apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wakaya-erp-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/raphaelparedes/wakaya-erp.git
    targetRevision: main
    path: ops/k8s/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: wakaya-erp-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

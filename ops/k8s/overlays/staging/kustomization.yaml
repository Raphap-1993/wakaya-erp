apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wakaya-erp-staging

resources:
  - ../../base

patches:
  - target: { kind: Deployment, name: wakaya-erp-api }
    patch: |
      - op: replace
        path: /spec/replicas
        value: 2

images:
  - name: ghcr.io/raphaelparedes/wakaya-erp
    newTag: staging
